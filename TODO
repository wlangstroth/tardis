* Interactive mode

* Batch export to Harvest (harvestapp.com)
* equivalent to sqlite3 ~/.tardis.db ".dump entries" | grep '^INSERT'
* Estimates on project tasks, with corresponding links
* Truncate and ellipsify project/description text in report
* New commands:
  * backup
  * report since
* update commend
